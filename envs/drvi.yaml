name: drvi
channels:
  #- pytorch
  #- nvidia
  - conda-forge
dependencies:
  - python=3.11
  - anndata<0.11
  - scanpy=1.9.5
  - zarr<3
  - scipy<1.13
  - dask
  - sparse
  - pytorch<2.4
  - pytorch-lightning<2.1
  - scvi-tools=1.0.4
  - jax<=0.4.20
  - jaxlib<=0.4.20
  - pip
  - pip:
    - jax==0.4.13  # needed for compatibility with MacOS
    - jaxlib==0.4.13  # needed for compatibility with MacOS
    - drvi-py
